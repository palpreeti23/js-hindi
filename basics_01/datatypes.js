"use strict"; //this treats all the js code in newer version 

// alert("preeti pal")

//we can not use alert in node js but in browser

/* 
some basic datatypes in js

Number => 2 to power 53
bigInt => for larger Number
String => " "
Boolean => true/false
null => standalon value //null is an object
let state = null;

undefined => undefined type
let dist;

symbol => unique value

*/

console.log(typeof null); //null is an object

console.log(typeof undefined);